<Page
    x:Class="TestApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="MainPageOnLoaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <TextBox Name="TextBox" Grid.Row="0" Width="200" Height="100"
                 VerticalAlignment="Center"
                 AutomationProperties.AutomationId="MyTextBox"/>
        <Button Content="Set 'CARAMBA' text to TextBox" Grid.Row="1"
                HorizontalAlignment="Center"
                Click="SetButtonOnClick"
                AutomationProperties.AutomationId="SetButton"/>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar IsSticky="False">
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Go" IsCompact="True"
                              Click="GoAppBarButtonOnClick"
                              AutomationProperties.AutomationId="GoAppBarButton"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>